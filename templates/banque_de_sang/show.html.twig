{% extends 'base.html.twig' %}

{% block title %}BanqueDeSang{% endblock %}

{% block body %}
    <h1>BanqueDeSang</h1>
    <div class="ab">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ banque_de_sang.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ banque_de_sang.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ banque_de_sang.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ banque_de_sang.tel }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <a href="{{ path('app_banque_de_sang_index') }}"><button class="btn">Back</button></a>

    <a href="{{ path('app_banque_de_sang_edit', {'id': banque_de_sang.id}) }}"><button class="btn">Edit</button></a>

    {{ include('banque_de_sang/_delete_form.html.twig') }}
{% endblock %}
