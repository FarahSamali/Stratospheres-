<form method="post" action="{{ path('app_notify_alert_delete', {'id': notify_alert.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notify_alert.id) }}">
    <button class="btn">Delete</button>
</form>
